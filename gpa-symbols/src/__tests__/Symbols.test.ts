import { HeavyCheckMark, Pencil, TrashCan, CrossMark, Plus, Warning, House, Spinner, Wrench, Flag, UpArrow, LeftArrow, DownArrow, DNA, InputNumbers, FourWayArrow, MagnifyingGlass, RightArrow, FastForwardButton, RewindButton, Questionmark, Scroll, PlayButton, StopButton, PauseButton } from '../index';

test('HeavyCheckMark', () => {
  expect(HeavyCheckMark).toHaveLength(2);
});
test('Pencil', () => {
    expect(Pencil).toHaveLength(2);
});
test('TrashCan', () => {
  expect(TrashCan).toHaveLength(3);
});
test('CrossMark', () => {
  expect(CrossMark).toHaveLength(1);
});
test('Plus', () => {
  expect(Plus).toHaveLength(1);
});
test('Warning', () => {
  expect(Warning).toHaveLength(2);
});
test('Spinner', () => {
    expect(Spinner).toHaveLength(2);
});
test('Wrench', () => {
  expect(Wrench).toHaveLength(2);
});
test('Flag', () => {
  expect(Flag).toHaveLength(2);
});
test('UpArrow', () => {
  expect(UpArrow).toHaveLength(2);
});
test('LeftArrow', () => {
    expect(LeftArrow).toHaveLength(1);
})
test('RightArrow', () => {
    expect(RightArrow).toHaveLength(1);
})
test('DownArrow', () => {
  expect(DownArrow).toHaveLength(2);
});
test('DNA', () => {
  expect(DNA).toHaveLength(2);
});
test('InputNumbers', () => {
  expect(InputNumbers).toHaveLength(2);
});
test('FourWayArrow', () => {
  expect(FourWayArrow).toHaveLength(1);
});
test('MagnifyingGlass', () => {
  expect(MagnifyingGlass).toHaveLength(2);
});
test('House', () => {
    expect(House).toHaveLength(2);
});
test('Scroll', () => {
    expect(Scroll).toHaveLength(2);
})
test('PlayButton', () => {
    expect(PlayButton).toHaveLength(2);
})
test('StopButton', () => {
    expect(StopButton).toHaveLength(2);
})
test('PauseButton', () => {
    expect(PauseButton).toHaveLength(2);
})
test('FastForwardButton', () => {
    expect(FastForwardButton).toHaveLength(1);
})
test('RewindButton', () => {
    expect(RewindButton).toHaveLength(1);
})
test('QuestionMark', () => {
    expect(Questionmark).toHaveLength(1);
})